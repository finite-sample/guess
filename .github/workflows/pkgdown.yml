name: pkgdown

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  release:
    types: [published]
  workflow_dispatch:

jobs:
  pkgdown:
    uses: gojiplus/r-canon/.github/workflows/reusable-pkgdown.yml@v1
