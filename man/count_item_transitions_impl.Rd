% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count-item-transitions.R
\name{count_item_transitions_impl}
\alias{count_item_transitions_impl}
\title{Count transitions for matched test items}
\usage{
count_item_transitions_impl(
  pre_test,
  post_test,
  ...,
  subgroup = NULL,
  response_schema = c("auto", "binary", "dk"),
  include_aggregate = FALSE,
  na_as = c("dk", "missing"),
  missing_action = c("omit", "error")
)
}
\arguments{
\item{pre_test}{Data frame containing one pre-test item per column.}

\item{post_test}{Data frame containing the corresponding post-test items.}

\item{...}{Must be empty. Its presence requires optional arguments to be
named.}

\item{subgroup}{Optional logical vector selecting observations to count.}

\item{include_aggregate}{Whether to append an `aggregate` row containing
column sums across items.}

\item{na_as}{Classification of `NA` responses: `"dk"` treats them as
observed don't-know responses and `"missing"` treats them as structural
missingness.}

\item{missing_action}{How to handle structural missingness: `"omit"`
excludes incomplete pairs and `"error"` rejects them.}
}
\value{
A visible integer matrix with items in rows and transition cells in
  columns. Row names preserve the item names from `pre_test`.
}
\description{
Apply [count_transitions()] to each named item shared by pre-test and
post-test data. Item columns are paired by name, not position.
}
\examples{
pre_test <- data.frame(
  item1 = c(1, 0, 0, 1, 0),
  item2 = c(1, NA, 0, 1, 0)
)
post_test <- data.frame(
  item1 = c(1, 1, 1, 1, 0),
  item2 = c(1, 1, 0, 1, 1)
)
count_item_transitions(pre_test, post_test)
}
\keyword{internal}
